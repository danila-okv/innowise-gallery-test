included:
  - Gallery

excluded:
  - Gallery/Resources
  - ${PWD}/**/*.generated.swift

line_length:
  warning: 120
  error: 200

disabled_rules:
  - trailing_whitespace

opt_in_rules:
  - empty_count
  - force_unwrapping
